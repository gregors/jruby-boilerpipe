require 'spec_helper'

describe Boilerpipe do
  it 'has a version number' do
    expect(Boilerpipe::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
